# Local package registry (build output)

Tarballs are **not** committed (`pkg.tar.gz` is gitignored). Generate with:

```bash
bash scripts/build_azlpack.sh
```

Layout: `<name>/<version>/pkg.tar.gz` (e.g. `azl-hello/1.0.0/pkg.tar.gz`).
