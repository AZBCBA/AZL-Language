# Gate F199: ::parse_tokens — top-level `name ( )` then `say …` → call|… then say|…
component ::azl.security {
  init {
  }
}

component ::boot.entry {
  init {
    link ::azl.security
    emit f199_parse with { }
  }
  behavior {
    listen for "f199_parse" {
      set ::tokens = []
      set ::l1 = "f199_fn ( )"
      set ::l2 = "say F199_MARK"
      set ::nx = "1"
      set ::tokens = ::tokens.concat(::tokenize_line(::l1, ::nx))
      set ::tokens.push({ type: "eol", value: ";", line: 1, column: 0 })
      set ::tokens = ::tokens.concat(::tokenize_line(::l2, ::nx))
      set ::tokens.push({ type: "eol", value: ";", line: 2, column: 0 })
      set ::_p = ::parse_tokens(::tokens)
      say ::ast.nodes
      say "P0_SEM_F199_OK"
    }
  }
}
