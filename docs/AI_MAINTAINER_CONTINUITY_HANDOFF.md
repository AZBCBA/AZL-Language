# AI & maintainer continuity — handoff (do not skip)

**Status:** **Permanent project memory** (git). AI sessions **do not** retain chat history across disconnects. **This file + linked docs are the continuity layer.** Re-read them at the **start** of substantive work.

**Audience:** Any human maintainer or **any AI assistant** joining cold.

**Last updated:** 2026-03-22

---

## 1. How we got here (reality, not story)

This direction was **not** invented in one message. It comes from a **long discussion**: vision (AZL leading; speed as real hardware outcomes; challenge legacy ML assumptions), **critical thinking** (what is proven vs aspirational), and **research grounding** (compression, codegen, information limits where relevant).

**Arrived-at consensus** is written in:

| Read first | Why |
|------------|-----|
| **[AZL_STRATEGIC_CONSENSUS_AND_EXECUTION_PLAN.md](AZL_STRATEGIC_CONSENSUS_AND_EXECUTION_PLAN.md)** | North star, phases, literal vs serving vs semantic, wedges W1–W3, harness vs language |
| **[PROJECT_COMPLETION_ROADMAP.md](PROJECT_COMPLETION_ROADMAP.md)** | P0–P5 spine, gates, what is **actually** queued; **§ P0.1** = ordered vertical-slice plan toward **`azl_interpreter.azl`** on the spine |
| **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** | **Process trace truth** — what runs today (C minimal, semantic Python, etc.) |

**Honesty about code vs narrative:**

| Doc | Role |
|-----|------|
| **[AZL_ENGINEERING_REALITY_AUDIT.md](AZL_ENGINEERING_REALITY_AUDIT.md)** | What the repo **really** does vs product language |
| **[LHA3_COMPRESSION_HONESTY.md](LHA3_COMPRESSION_HONESTY.md)** | Heuristic retention vs **literal** byte codecs — do not conflate |
| **[AZL_LITERAL_CODEC_CONTAINER_V0.md](AZL_LITERAL_CODEC_CONTAINER_V0.md)** | **Exact** tier wire format + **`CODEC_*`** errors (normative); decoder implementation = subsequent harness work |

---

## 2. What is reality today vs what we are building

- **Reality today:** Partial spine, narrow interpreters, docs and `.azl` files that sometimes **read** more complete than the **default trace**. Rust may live **off-repo**; **0** `*.rs` in-tree per audit.
- **Target (not yet fully real):** Native **AOT/codegen** backbone; **literal** codecs **where Exact is claimed**; **serving/quantized** paths where scale matters; **LHA3/policy** as semantic layer; **bootstrap** then **self-host**; **CLI** install story; **tests/harness** separate from language with **explicit errors** per **[ERROR_SYSTEM.md](ERROR_SYSTEM.md)**.
- **Release verify:** **`make verify`** runs **`scripts/verify_azl_interpreter_semantic_spine_smoke.sh`** (step **3**) and **`scripts/verify_azl_interpreter_semantic_spine_behavior_smoke.sh`** (step **4**) after native gates — **P0.1b** proves **`init`** on the Python spine with stub **`::azl.security`**; **P0.1c** proves **nine** **`emit interpret`** passes (dual **`say x`** + **`(cache hit)`**, third two-line + fourth three-line + fifth four-line **`say`** with **`AZL_SPINE_DEPTH_*`**, **`AZL_SPINE_TRIPLE_*`**, **`Q5a`**–**`Q5d`**, duplicate **`say 'AZL_S6_ONLY'`** + **second** tok/parse **`(cache hit)`** pair, eighth **`AZL_S8_MARK`**, ninth **`set ::…`** + **`say`** → **`AZL_SPINE_P9_SET_LINE`**), **`execute_complete`** (**ERROR_SYSTEM** **548–562**, **611**). See **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** **P0.1b** / **P0.1c**, **[CONNECTIVITY_AUDIT.md](CONNECTIVITY_AUDIT.md)** steps **3–4**.

**Any assistant must treat “target” as a roadmap, not as shipped fact** — unless a **gate, test, or trace** proves it.

---

## 3. Instructions to your future AI self (anti-loop, anti-fake)

**Do:**

1. **Open the three docs in §1** before large refactors or “architecture” answers.
2. **Trace the process** (spine doc) when claiming what “runs” or “is canonical.”
3. **Separate** harness from language; **name errors**; **no silent success** on failure.
4. **Match vision to implementation:** if adding **literal** compression, add **round-trip proof** in harness — update **LHA3** honesty docs if behavior changes.
5. **Respect maintainer rules:** production mindset, error system, fix originals — no duplicate “enhanced/simple” file trees.

**Do not:**

1. **Loop** the same abstract lecture (e.g. entropy / lossless limits) **unless** the user is making a **specific impossible lossless claim** — then **one precise sentence**, then return to **their** engineering goal (AOT, policy, codecs, spine).
2. **Imply** “pure AZL / no Rust / interpreter replaces everything” **without** aligning to **RUNTIME_SPINE_DECISION.md** — that **wastes** maintainer time and **erodes trust**.
3. **Invent** milestones, green CI, or “done” states **not** backed by scripts/gates in repo.
4. **Conflate** marketing names (RepertoireField, quantum labels) with **one unified implementation** without reading **GPU_NEURAL_SURFACE_MAP** + audit.

---

## 4. Maintainer vision (one paragraph, stable)

**AZL** is the **leading** language for the system — **not Python-shaped** long-term. **Speed** means **measured** outcomes on CPU/GPU/VRAM/IO. **Primary backbone:** **compile to native / AOT** (e.g. LLVM-class), with honest **bootstrap**; **self-host** is the **same** project, staged. **Compression/memory:** **Exact** artifacts need **literal, proven** codecs + **errors**; **scale** needs **lossy/structural** paths + **metrics**; **LHA3** owns **semantic policy** — do not oversell heuristics as byte codecs. **Research** challenges dense weights / flat bytes / generic compression **as wedges** with **hypotheses and tests**. **Shipping:** CLI / GitHub-style workflow, not desktop-first.

---

## 5. If you are an AI and only remember one thing

**The repo + these linked documents are the memory.**  
**Re-read this handoff and the strategic consensus doc at session start.**  
**Aspiration is real work; lies and loops are not.**

---

## 6. Changelog (this file)

| Date | Change |
|------|--------|
| 2026-03-24 | Gates **F184**, **F185** — inner **`name ( )`** in **`listen { … }`** → **`listen|<ev>|call|<name>`** (**`p0_semantic_parse_tokens_listen_call.azl`**; **801–803**); **`execute_ast`** **`listen|…|call|…`** stub via **`EXEC_AST_STUB_CALL`** + **`exec_ast_fn_get`** (**`p0_semantic_execute_ast_listen_call_stub.azl`**; **804–806**). Python **`process_events`** uses **`_execute_ast_fn_reg`** for **`call`** stub lookup. |
| 2026-03-24 | Gates **F186**, **F187** — inner **`name ( '…' )`** → **`listen|…|call|…|payload`** (**`p0_semantic_parse_tokens_listen_call_string_arg.azl`**; **807–809**); **`execute_ast`** **`listen|…|call|…|arg`** stores arg in **`set_key`** / stub tuple **`sa2`**; dispatch prints fn **`say`** then arg (**`p0_semantic_execute_ast_listen_call_arg_stub.azl`**; **810–812**). |
| 2026-03-24 | Gates **F188**, **F189** — top-level **`parse_tokens`** **`name ( '…' )`** → **`call|…|…`** (**`p0_semantic_parse_tokens_top_call_string_arg.azl`**; **813–815**); **`execute_ast_call_line`** / **`_builtin_execute_ast_run_lines`** print fn registry line then optional arg (**`p0_semantic_execute_ast_top_call_arg_stub.azl`**; **816–818**). |
| 2026-03-24 | Gates **F190**, **F191** — **`name ( ::id )`** as **`call`** arg (identifier token): listen-inner (**`p0_semantic_parse_tokens_listen_call_ident_arg.azl`**; **819–821**), top-level (**`p0_semantic_parse_tokens_top_call_ident_arg.azl`**; **822–824**). |
| 2026-03-24 | Gates **F192**, **F193** — dual empty **`call`** regression: listen body (**`p0_semantic_parse_tokens_listen_dual_call.azl`**; **825–827**), top-level (**`p0_semantic_parse_tokens_dual_top_call.azl`**; **828–830**). |
| 2026-03-24 | Gates **F194**, **F195** — dual **`call`** with string payload (**`p0_semantic_parse_tokens_listen_dual_call_string.azl`**; **831–833**; **`p0_semantic_parse_tokens_dual_top_call_string.azl`**; **834–836**). |
| 2026-03-24 | Gates **F196**, **F197** — dual **`call`** with identifier payload (**`p0_semantic_parse_tokens_listen_dual_call_ident.azl`**; **837–839**; **`p0_semantic_parse_tokens_dual_top_call_ident.azl`**; **840–842**). |
| 2026-03-24 | Gate **F183** — **`::parse_tokens`** inner **`let ::… = …`** inside **`listen { … }`** → **`listen|<ev>|let|::…|…`** (same RHS rules as inner **`set`**). **`execute_ast`** registers **`listen|…|let|…`** via existing **set** stub (**`var_set`**). **`p0_semantic_parse_tokens_listen_let_say.azl`**; exits **798–800**. |
| 2026-03-24 | Gate **F182** — **`::parse_tokens`** nested **`listen for inner { … }`** inside outer **`listen { … }`**: inner statements emit **`listen|<inner>|…`** rows; sibling outer statements use **`listen|<outer>|…`** (**`p0_semantic_parse_tokens_listen_nested_say.azl`**; exits **795–797**). C **`parse_listen_inner_nested_listen`**; Python **`_parse_listen_nested_listen_block`**. |
| 2026-03-24 | Gate **F181** — **`::execute_ast`** stub dispatch for **`listen|…|return[|…]`** / **`memory|listen|…|return[|…]`** (payload optional; bare **`return`** prints nothing on dispatch). **`p0_semantic_execute_ast_memory_listen_return_stack.azl`**; exits **792–794**. C **`EXEC_AST_STUB_RETURN`** + **`exec_ast_stub_register_return`**; Python **`_execute_ast_try_listen_stub`** + **`process_events`** **`return`** kind. |
| 2026-03-24 | Gates **F177**, **F178** — **`listen { return … }`** → **`listen|…|return[|payload]`** (**786–788**, **`p0_semantic_parse_tokens_listen_return.azl`**); one-buffer **`memory say …`** then **`listen { say … }`** (**789–791**, **`p0_semantic_parse_tokens_memory_then_listen.azl`**). C/Python **`parse_listen_inner_body`** / **`_parse_listen_inner_to_row`**; top-level parse skips **`call|return`** via keyword list. |
| 2026-03-24 | Gate **F176** — **`::parse_tokens`** **`listen { if ( true \| false \| 1 \| 0 ) { say … } }`** → **`listen|…|say|…`** when truthy; **empty** inner line when false (**`p0_semantic_parse_tokens_listen_if_say.azl`** fixture; exits **783–785**). C **`parse_listen_inner_body`** + Python **`_parse_listen_inner_to_row`**; Python **`listen`** loop appends only non-empty inner rows. |
| 2026-03-24 | Gates **F173–F175**, **F179**, **F180** — **`::parse_tokens`** multi-statement **`listen { … }`** shapes: **set** + **`emit … with`** multi-pair (**768–770**); **say** + **set** + **emit** (**771–773**); **emit** then **set** (**774–776**); quoted inner event + **`with`** (**777–779**); **`with`** value token **`::…`** (**780–782**). **`docs/P0_INDEPENDENCE_QUEUE.md`** tracks the 100-item backlog. Parity ceiling docs **F2–F180** (+ **F177–F178** exits **786–791**). |
| 2026-03-23 | Gate **F167** — **`::parse_tokens`** multi-statement **`listen { say …` / `emit …` / `}`**; bare inner **`emit`** ends at **`eol`** (**`p0_semantic_parse_tokens_listen_say_emit.azl`**; exits **615–617**). Parity ceiling docs **F2–F167**; **Open next** **F168+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F166** — **`::parse_tokens`** multi-statement **`listen { say …` / `say … }`** → **two** **`listen|…|say|…`** rows (**`p0_semantic_parse_tokens_listen_multi_say.azl`**; exits **612–614**). Inner **`say`/`set`** rows end at **`eol`** or **`}`**; C **`builtin_parse_tokens_nodes`** listen loop must not wipe **`acc`** when **`j2 == np`** after a successful inner parse. Parity ceiling docs **F2–F166**; **Open next** **F167+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F165** — **`::parse_tokens`** **`listen … { emit … with { a: b, c: d } }`** → **`listen|…|emit|…|with|a|b|c|d`** (**`p0_semantic_parse_tokens_listen_emit_multi_with.azl`**; exits **608–610**). **Minimal** **`Var.v`** **512** / **`MAX_VAR_VALUE_LEN` 511** (C / Python) so **`tz|`** concat buffers >255 bytes parse correctly. |
| 2026-03-22 | Gate **F164** — **`::parse_tokens`** **`memory emit … with { a: b, c: d }`** → **`memory|emit|…|with|a|b|c|d`** (**`p0_semantic_parse_tokens_memory_emit_multi_with.azl`**; exits **605–607**). Parity ceiling docs **F2–F164**; **Open next** **F165+** (**`listen|…|emit|…|with`** multi-pair from tokens) in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F163** — **`::parse_tokens`** **`memory emit … with { k: v }`** → **`memory|emit|…|with|…`** (**`p0_semantic_parse_tokens_memory_emit_with.azl`**; exits **602–604**). Parity ceiling docs **F2–F163**; **Open next** **F164+** (**`memory emit … with`** multi-pair from tokens) in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F162** — **`::parse_tokens`** bare **`memory emit …`** → **`memory|emit|…`** (**`p0_semantic_parse_tokens_memory_emit.azl`**; exits **599–601**). Parity ceiling docs **F2–F162**; **Open next** **F163+** (**`memory emit … with`** from tokens) in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | **P0.1 gap matrix** — **[TIER_B_BACKLOG.md](TIER_B_BACKLOG.md)** § **P0.1 — Semantic / runtime gaps (explicit, narrow claims)** + roadmap pointer; **Phase F** = full **behavior** claim only when that table’s criteria are met. Gate **F161** — **`::parse_tokens`** **`memory set …`** → **`memory|set|…`** (**`p0_semantic_parse_tokens_memory_set.azl`**; exits **596–598**). Parity ceiling docs **F2–F161**; **Open next** **F162+** (**`memory emit`** from tokens) in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F160** — **`::parse_tokens`** **`memory say …`** → **`memory|say|…`** (**`p0_semantic_parse_tokens_memory_say.azl`**; exits **593–595**). Parity ceiling docs **F2–F160**; **Open next** **F161+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (parse slice: **`memory set`** / **`memory emit`** fixtures). |
| 2026-03-22 | Gate **F148** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p148.a`** then **eleven** bare **`memory|emit|…`** (**`e1`…`e11`**, FIFO) then **`component|p148.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_undeca_bare_emit_component_say.azl`**; exits **545–547**, **P0execpreilcomponentmemoryundecabareemitcomponentsay**). Parity range docs now **F2–F148**; **Open next** **F149+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F147** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p147.a`** then **ten** bare **`memory|emit|…`** (**`e1`…`e10`**, FIFO) then **`component|p147.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_deca_bare_emit_component_say.azl`**; exits **542–544**, **P0execpreilcomponentmemorydecabareemitcomponentsay**). Parity range docs now **F2–F147**; **Open next** **F148+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F146** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p146.a`** then **nine** bare **`memory|emit|…`** (**`e1`…`e9`**, FIFO) then **`component|p146.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_nona_bare_emit_component_say.azl`**; exits **539–541**, **P0execpreilcomponentmemorynonabareemitcomponentsay**). Parity range docs now **F2–F146**; **Open next** **F147+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F145** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p145.a`** then **eight** bare **`memory|emit|…`** (**`e1`…`e8`**, FIFO) then **`component|p145.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_octa_bare_emit_component_say.azl`**; exits **536–538**, **P0execpreilcomponentmemoryoctabareemitcomponentsay**; **`import|i`** + short event names). Parity range docs now **F2–F145**; **Open next** **F146+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F144** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p144.a`** then **seven** bare **`memory|emit|…`** (**`e1`…`e7`**, FIFO) then **`component|p144.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_hepta_bare_emit_component_say.azl`**; exits **533–535**, **P0execpreilcomponentmemoryheptabareemitcomponentsay**; **`import|i`** + short event names). Parity range docs now **F2–F144**; **Open next** **F145+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F143** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p143.a`** then **hexa** **`memory|emit|…`** (**`with`** **`a`** / **bare** **`e2`** / **`with`** **`b`** / **bare** **`e4`** / **`with`** **`c`** / **bare** **`e6`**, FIFO) then **`component|p143.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_hexa_mixed_with_bare_with_bare_with_bare_emit_component_say.azl`**; exits **530–532**, **P0execpreilcomponentmemoryhexamixedwithbarewithbarewithbareemitcomponentsay**). Parity range docs now **F2–F143**; **Open next** **F144+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F142** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p142.a`** then **hexa** **`memory|emit|…`** (**bare** **`e1`** / **`with`** **`a`** / **bare** **`e3`** / **`with`** **`b`** / **bare** **`e5`** / **`with`** **`c`**, FIFO) then **`component|p142.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_hexa_mixed_bare_with_bare_with_bare_with_emit_component_say.azl`**; exits **527–529**, **P0execpreilcomponentmemoryhexamixedbarewithbarewithbarewithemitcomponentsay**; short **`import|i`** / **`e1`…`e6`** so **`::ast.nodes`** fits **255**-byte minimal bound). Parity range docs now **F2–F142**; **Open next** **F143+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F141** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p141.a`** then **penta** **`memory|emit|…`** (**`with`** **`fk141a`** / **bare** **`f141_e2`** / **`with`** **`fk141b`** / **bare** **`f141_e4`** / **`with`** **`fk141c`**, FIFO) then **`component|p141.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_penta_mixed_with_bare_with_bare_with_emit_component_say.azl`**; exits **524–526**, **P0execpreilcomponentmemorypentamixedwithbarewithbarewithemitcomponentsay**). Parity range docs now **F2–F141**; **Open next** **F142+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F140** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p140.a`** then **penta** **`memory|emit|…`** (**bare** **`f140_e1`** / **`with`** **`fk140a`** / **bare** **`f140_e3`** / **`with`** **`fk140b`** / **bare** **`f140_e5`**, FIFO) then **`component|p140.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_penta_mixed_bare_with_bare_with_bare_emit_component_say.azl`**; exits **521–523**, **P0execpreilcomponentmemorypentamixedbarewithbarewithbareemitcomponentsay**). Parity range docs now **F2–F140**; **Open next** **F141+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F139** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p139.a`** then **two** **`memory|emit|…|with|…`** (**`fk139a`** / **`fk139b`**) then **two** bare **`memory|emit|…`** (**`f139_e3`** / **`f139_e4`**, FIFO) then **`component|p139.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_quad_mixed_with_with_bare_bare_emit_component_say.azl`**; exits **518–520**, **P0execpreilcomponentmemoryquadmixedwithwithbarebareemitcomponentsay**). Parity range docs now **F2–F139**; **Open next** **F140+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F138** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p138.a`** then **bare** **`memory|emit|f138_e1`** then **`memory|emit|…|with|…`** (**`fk138a`**) then **bare** **`memory|emit|f138_e3`** then **`memory|emit|…|with|…`** (**`fk138b`**) (FIFO) then **`component|p138.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_quad_mixed_bare_with_bare_with_emit_component_say.azl`**; exits **515–517**, **P0execpreilcomponentmemoryquadmixedbarewithbarewithemitcomponentsay**). Parity range docs now **F2–F138**; **Open next** **F139+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F137** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p137.a`** then **`memory|emit|…|with|…`** (**`fk137a`**) then **two** bare **`memory|emit|…`** (**`f137_e2`** / **`f137_e3`**) then **`memory|emit|…|with|…`** (**`fk137b`**) (FIFO) then **`component|p137.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_quad_mixed_with_bare_bare_with_emit_component_say.azl`**; exits **512–514**, **P0execpreilcomponentmemoryquadmixedwithbarebarewithemitcomponentsay**). Parity range docs now **F2–F137**; **Open next** **F138+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F136** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p136.a`** then **bare** **`memory|emit|f136_e1`** then **two** **`memory|emit|…|with|…`** (**`fk136a`** / **`fk136b`**) then **bare** **`memory|emit|f136_e4`** (FIFO) then **`component|p136.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_quad_mixed_bare_with_with_bare_emit_component_say.azl`**; exits **509–511**, **P0execpreilcomponentmemoryquadmixedbarewithwithbareemitcomponentsay**). Parity range docs now **F2–F136**; **Open next** **F137+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F135** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p135.a`** then **bare** **`memory|emit|f135_e1`** then **`memory|emit|f135_e2|with|fk135|F135_PAY_MID`** then **bare** **`memory|emit|f135_e3`** (FIFO) then **`component|p135.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_triple_mixed_bare_with_bare_emit_component_say.azl`**; exits **506–508**, **P0execpreilcomponentmemorytriplemixedbarewithbareemitcomponentsay**). Parity range docs now **F2–F135**; **Open next** **F136+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F134** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p134.a`** then **`memory|emit|f134_e1|with|fk134a|F134_PAY_A`** then **bare** **`memory|emit|f134_e2`** then **`memory|emit|f134_e3|with|fk134b|F134_PAY_B`** (FIFO) then **`component|p134.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_triple_mixed_emit_component_say.azl`**; exits **503–505**, **P0execpreilcomponentmemorytriplemixedemitcomponentsay**). Parity range docs now **F2–F134**; **Open next** **F135+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F133** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p133.a`** then **`memory|emit|f133_e1|with|fk133|F133_PAYLOAD`** then **bare** **`memory|emit|f133_e2`** (FIFO) then **`component|p133.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_mixed_with_bare_emit_component_say.azl`**; exits **500–502**, **P0execpreilcomponentmemorymixedwithbareemitcomponentsay**). Parity range docs now **F2–F133**; **Open next** **F134+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F132** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p132.a`** then **bare** **`memory|emit|f132_e1`** then **`memory|emit|f132_e2|with|fk132|F132_PAYLOAD`** (FIFO) then **`component|p132.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_mixed_bare_with_emit_component_say.azl`**; exits **497–499**, **P0execpreilcomponentmemorymixedbarewithemitcomponentsay**). Parity range docs now **F2–F132**; **Open next** **F133+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F131** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p131.a`** then **three** bare **`memory|emit|…`** (**`f131_e1`** … **`f131_e3`**, FIFO) then **`component|p131.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_triple_bare_emit_component_say.azl`**; exits **494–496**, **P0execpreilcomponentmemorytriplebareemitcomponentsay**). Parity range docs now **F2–F131**; **Open next** **F132+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F130** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p130.a`** then **two** bare **`memory|emit|…`** (**`f130_e1`** / **`f130_e2`**, FIFO) then **`component|p130.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_dual_bare_emit_component_say.azl`**; exits **491–493**, **P0execpreilcomponentmemorydualbareemitcomponentsay**). Parity range docs now **F2–F130**; **Open next** **F131+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F129** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p129.a`** then **bare** **`memory|emit|f129_bare`** (no **`|with|…`**, **F105**-shaped drain) then **`component|p129.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_bare_emit_component_say.azl`**; exits **488–490**, **P0execpreilcomponentmemorybareemitcomponentsay**). Parity range docs now **F2–F129**; **Open next** **F130+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F128** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p128.a`** then **three** **`memory|emit|…|with|…`** (**`f128_e1`** … **`f128_e3`**, FIFO drain) then **`component|p128.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_triple_emit_component_say.azl`**; exits **485–487**, **P0execpreilcomponentmemorytripleemitcomponentsay**). Parity range docs now **F2–F128**; **Open next** **F129+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F127** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p127.a`** then **two** **`memory|emit|…|with|…`** (**`f127_e1`** / **`f127_e2`**, FIFO drain) then **`component|p127.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_dual_emit_component_say.azl`**; exits **482–484**, **P0execpreilcomponentmemorydualemitcomponentsay**). Parity range docs now **F2–F127**; **Open next** **F128+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F126** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p126.a`** then **`memory|emit|f126_mid|with|key|…`** (queue + drain) then **`component|p126.b`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_emit_component_say.azl`**; exits **479–481**, **P0execpreilcomponentmemoryemitcomponentsay**). Parity range docs now **F2–F126**; **Open next** **F127+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F125** — **`execute_ast`** preloop **`import|/`link|`** then **three** **`component|p125.(a|b|c)`** rows with **`memory|say|…`** between each segment (**`p0_semantic_execute_ast_preloop_three_component_memory_say.azl`**; exits **476–478**, **P0execpreilthreecomponentmemorysay**). Parity range docs now **F2–F125**; **Open next** **F126+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F124** — **`execute_ast`** preloop **`import|/`link|`** then **`component|p124.alpha`** then **`memory|say|…`** then **`component|p124.beta`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_two_component_memory_say.azl`**; exits **473–475**, **P0execpreiltwocomponentmemorysay**). Parity range docs now **F2–F124**; **Open next** **F125+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F123** — **`execute_ast`** preloop **`import|/`link|`** then **`component|`** + dual **`memory|set|…`** then **F119**-shaped stacked **`memory|listen|…|say|…`** + dual **`memory|emit|…`** + **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_component_memory_set_listen_stack.azl`**; exits **470–472**, **P0execpreilcomponentmemorysetlistenstack**). Parity range docs now **F2–F123**; **Open next** **F124+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F122** — **`execute_ast`** preloop **`import|/`link|`** then **`emit|…|with|…`** then **F119**-shaped stacked **`memory|listen|…|say|…`** + dual **`memory|emit|…`** + **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_emit_then_memory_listen_stack_say.azl`**; exits **467–469**, **P0execpreilemitmemorylistenstack**). Parity range docs now **F2–F122**; **Open next** **F123+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F121** — **`execute_ast`** preloop **`import|/`link|`** then **`say|`** then **F119**-shaped stacked **`memory|listen|…|say|…`** + dual **`memory|emit|…`** + **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_say_then_memory_listen_stack_say.azl`**; exits **464–466**, **P0execpreilsaymemorylistenstack**). Parity range docs now **F2–F121**; **Open next** **F122+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F120** — **`execute_ast`** preloop **`import|/`link|`** then **F119**-shaped stacked **`memory|listen|…|say|…`** + dual **`memory|emit|…`** + **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_memory_listen_stack_say.azl`**; exits **461–463**, **P0execpreilmemorylistenstack**). Parity range docs now **F2–F120**; **Open next** **F121+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F119** — **`execute_ast`** stacked **`memory|listen|…|say|…`** stubs + dual **`memory|emit|…`** + **`memory|say|…`** (**`p0_semantic_execute_ast_memory_listen_stack_say.azl`**; exits **458–460**, **P0exectreememorylistenstack**). Parity range docs now **F2–F119**; **Open next** **F120+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F118** — **`execute_ast`** preloop **`import|/`link|`** then **F117**-shaped **`memory|listen|…|emit|…|with`** multi-pair + **`memory|emit|…`** + **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_memory_listen_emit_multi_with_say.azl`**; exits **455–457**, **P0execpreilmemorylistenemitwithmulti**). Parity range docs now **F2–F118**; **Open next** **F119+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F117** — **`execute_ast`** **`memory|listen|…|emit|…|with`** multi-pair stub + **`memory|emit|…`** + **`memory|say|…`** (**`p0_semantic_execute_ast_memory_listen_emit_multi_with_say.azl`**; exits **452–454**, **P0exectreememorylistenemitwithmulti**). Parity range docs now **F2–F117**; **Open next** **F118+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F116** — **`execute_ast`** **`memory|listen|…|emit|…|with|…`** stub + **`memory|emit|…`** + **`memory|say|…`** (**`p0_semantic_execute_ast_memory_listen_emit_with_say.azl`**; exits **449–451**, **P0exectreememorylistenemitwith**). Parity range docs now **F2–F116**; **Open next** **F117+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F115** — **`execute_ast`** **`memory|listen|…`** stub + **`memory|emit|…`** + **`memory|say|…`** (**`p0_semantic_execute_ast_memory_listen_emit_say.azl`**; exits **446–448**, **P0exectreememorylisten**). Parity range docs now **F2–F115**; **Open next** **F116+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F114** — **`execute_ast`** **`import|/`link|`** preloop then **`emit|…|with|…`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_emit_then_memory_say.azl`**; exits **443–445**, **P0execpreilemitmemory**). Parity range docs now **F2–F114**; **Open next** **F115+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F113** — **`execute_ast`** **`import|/`link|`** preloop then **`say|`** then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_say_then_memory_say.azl`**; exits **440–442**, **P0execpreilsaymemory**). Parity range docs now **F2–F113**; **Open next** **F114+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E**). |
| 2026-03-22 | Gate **F112** — **`execute_ast`** **`import|/`link|`** preloop then **`memory|say|…`** (**`p0_semantic_execute_ast_preloop_then_memory_say.azl`**; exits **437–439**, **P0execpreilmemory**). Parity range docs now **F2–F112**; **Open next** **F113+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** (vertical slice **E** toward real **`execute`** + memory ordering). |
| 2026-03-22 | Gate **F111** — **`execute_ast`** **`memory|set|…`** → multi-pair **`memory|emit|…|with|…`** → **`memory|say|…`** (**`p0_semantic_execute_ast_memory_mixed_emit_multi_with_order.azl`**; exits **434–436**, **P0exectreememorymixedemitwithmulti**). Parity range docs now **F2–F111**; **Open next** **F112+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F110** — **`execute_ast`** **`memory|set|…`** → **`memory|emit|…|with|…`** → **`memory|say|…`** (**`p0_semantic_execute_ast_memory_mixed_emit_with_order.azl`**; exits **431–433**, **P0exectreememorymixedemitwith**). Parity range docs now **F2–F110**; **Open next** **F111+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F109** — **`execute_ast`** mixed **`memory|set|…`** → **`memory|emit|…`** → **`memory|say|…`** (**`p0_semantic_execute_ast_memory_mixed_order.azl`**; exits **428–430**, **P0exectreememorymixed**). Parity range docs now **F2–F109**; **Open next** **F110+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F108** — **`execute_ast`** multi-row **`memory|say|…`** + trailing **`say|`** order (**`p0_semantic_execute_ast_memory_multi_row_order.azl`**; exits **425–427**, **P0exectreememorymultirow**). Parity range docs now **F2–F108**; **Open next** **F109+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F107** — **`execute_ast`** **`memory|emit|…|with`** multi-pair stub (**`p0_semantic_execute_ast_memory_emit_multi_with_step.azl`**; exits **422–424**, **P0exectreememoryemitwithmulti**). Parity range docs now **F2–F107**; **Open next** **F108+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F106** — **`execute_ast`** **`memory|emit|…|with|…`** stub (**`p0_semantic_execute_ast_memory_emit_with_step.azl`**; exits **419–421**, **P0exectreememoryemitwith**). Parity range docs now **F2–F106**; **Open next** **F107+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F105** — **`execute_ast`** **`memory|emit|…`** stub (**`p0_semantic_execute_ast_memory_emit_step.azl`**; exits **416–418**, **P0exectreememoryemit**). Parity range docs now **F2–F105**; **Open next** **F106+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F104** — **`execute_ast`** **`memory|set|…`** / **`memory|say|…`** stub (**`p0_semantic_execute_ast_memory_set_step.azl`**; exits **413–415**, **P0exectreememory**). Parity range docs now **F2–F104**; **Open next** **F105+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | **C minimal `.push` `tz|…` row:** **`parse_push_tz_object`** uses a **512-byte** **`snprintf`** scratch buffer + bounded **`memcpy`** into **`seg`** (see **P0tz** note in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**) — clears **`-Wformat-truncation`** on **`gcc`**; deeper spine queue **F124+** (post-**F123**) unchanged by this hygiene fix. |
| 2026-03-22 | Gate **F103** — **`execute_ast`** **`listen|…|emit|…|with`** multi-pair stub (**`p0_semantic_execute_ast_listen_emit_multi_with_stub.azl`**; exits **410–412**, **P0exectreelistenemitwithmulti**). Parity range docs now **F2–F103**; **Open next** **F104+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F102** — **`execute_ast`** **`listen|…|emit|…|with|…`** stub (**`p0_semantic_execute_ast_listen_emit_with_stub.azl`**; exits **407–409**, **P0exectreelistenemitwith**). Parity range docs now **F2–F102**; **Open next** **F103+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F101** — **`execute_ast`** **`listen|…|set|::global|value`** stub (**`p0_semantic_execute_ast_listen_set_stub.azl`**; exits **404–406**, **P0exectreelistenset**). Parity range docs now **F2–F101**; **Open next** **F102+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-22 | Gate **F100** — **`execute_ast`** **`listen|…|emit|inner`** stub (**`p0_semantic_execute_ast_listen_emit_stub.azl`**; exits **401–403**, **P0exectreelistenemit**). Parity range docs now **F2–F100**; **Open next** **F101+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**. |
| 2026-03-21 | **P0.1 vertical-slice execution order** (phases **A–F**) in **[PROJECT_COMPLETION_ROADMAP.md](PROJECT_COMPLETION_ROADMAP.md)** § **P0.1** + checklist in **[TIER_B_BACKLOG.md](TIER_B_BACKLOG.md)**; pointers from **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**, strategic plan, **[INTEGRATION_VERIFY.md](INTEGRATION_VERIFY.md)**, **[AGENTS.md](../AGENTS.md)**. |
| 2026-03-21 | Native semantic parity **F5–F73** on fixtures (**ERROR_SYSTEM** § Native gates; **271** skipped — literal codec): through **F71** **`split_chars()` + char `for`** (**311–313**, **P0u**); **F72** **`set ::buf.push(…)`** (**314–316**, **P0push**); **F73** int **`-`** + **`.length`** (**317–319**, **P0sub**). New behavior = **`p0_semantic_*.azl`** + C + Python + **`check_azl_native_gates.sh`** + doc rows — no silent exits. |
| 2026-03-22 | Gate **F94** — **`execute_ast`** **`emit|…`** bare emit + drain (**`p0_semantic_execute_ast_emit_step.azl`**; exits **383–385**, **P0exectreeemit**). **Open next:** **`component|`** / **`emit|…|with`** / preloop — **F95+**. Cross-docs + **CHANGELOG** updated. |
| 2026-03-22 | Gate **F93** — **`::execute_ast(::ast, ::scope)`** stub (**`p0_semantic_execute_ast_tree_walk.azl`**; **`say|…`** steps in **`::ast.nodes`**; exits **380–382**, **P0exectree**). |
| 2026-03-21 | Gates **F90–F92** — **`AZL_USE_VM=1`** stub **`::vm_compile_ast`** / **`::vm_run_bytecode_program`** (**`p0_semantic_execute_vm_*.azl`**; exits **371–379**, **P0execvm**). |
| 2026-03-21 | Gate **F89** — execute **`::ast.nodes`** preloop + **`&&`**, **`for-in`** inside listener **`if`** (**`p0_semantic_execute_ast_nodes_preloop.azl`**; exits **368–370**, **P0execpre**). |
| 2026-03-21 | Gate **F88** — **`halt_execution`** listener (**`p0_semantic_halt_execution_listener.azl`**; exits **365–367**, **P0halt**). |
| 2026-03-22 | Native semantic parity **F74–F87** (tokenize + parse + execute stub): **F74–F82** tokenize (**323–349**); **F83–F85** parse (**350–358**); **F86–F87** execute payload + **`execute_complete`**, **`AZL_USE_VM`** env-off (**359–364**, **P0execpayload** / **P0usevmoff**). Gate **F87** requires **`AZL_USE_VM` unset** in native gate runner. **`make verify`** green; cross-docs + **CHANGELOG** updated. |
| 2026-03-20 | Initial continuity handoff after long alignment discussion; links strategic plan + spine + audit. |

---

## 7. Native F-gate hygiene (extending **F** parity)

When extending **`scripts/check_azl_native_gates.sh`** with a new semantic parity slice (**F(n+1)** after the current ceiling in **AGENTS.md**):

1. **Fixture** — Add **`azl/tests/p0_semantic_*.azl`** using the same harness patterns as siblings (**`env -u AZL_USE_VM`**, **`link`**, **`execute_complete`**, stable stdout markers). **Byte-match C vs Python** (`tools/azl_interpreter_minimal` vs `tools/azl_runtime_spine_host.py`) *before* locking the **`awk`** expectations in the gate script.
2. **Exit codes** — Reserve a **new** triple (**C failure / Python failure / stdout mismatch**). Append rows to **[ERROR_SYSTEM.md](ERROR_SYSTEM.md)** § Native gates and extend the long **F5–F*n*** inline map in the same edit; keep **[AGENTS.md](../AGENTS.md)** exit map **identical** to that line.
3. **Spine + process docs** — New **P0…** paragraph and **Open next F(n+1)+** in **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)**; bump **F2–F*n*** everywhere it denotes the **current** ceiling (**[PROJECT_COMPLETION_ROADMAP.md](PROJECT_COMPLETION_ROADMAP.md)** principle + phases **A**/**E**, **[TIER_B_BACKLOG.md](TIER_B_BACKLOG.md)**, **[CONTRIBUTING.md](CONTRIBUTING.md)** parity *and* numeric tail, **[CONNECTIVITY_AUDIT.md](CONNECTIVITY_AUDIT.md)**, **[RELEASE_READY.md](../RELEASE_READY.md)**, **[AZL_STRATEGIC_CONSENSUS_AND_EXECUTION_PLAN.md](AZL_STRATEGIC_CONSENSUS_AND_EXECUTION_PLAN.md)**, **[README.md](README.md)**, **[AZL_DOCUMENTATION_CANON.md](AZL_DOCUMENTATION_CANON.md)**, **[AZL_ENGINEERING_REALITY_AUDIT.md](AZL_ENGINEERING_REALITY_AUDIT.md)**); add a row here (§6) and **[CHANGELOG.md](../CHANGELOG.md)**.
4. **Runtime sources** — If the slice is **`execute_ast`** **`memory|`** / preloop shape, extend the **F104–F*n*** comment band in **`tools/azl_interpreter_minimal.c`** and **`tools/azl_semantic_engine/minimal_runtime.py`** in the same change set.

**Authoritative parity ceiling and exit map:** **[AGENTS.md](../AGENTS.md)** (semantic spine section). **Spine queue (“Open next”):** **[RUNTIME_SPINE_DECISION.md](RUNTIME_SPINE_DECISION.md)** — do not let this §7 duplicate go stale; update the two sources of truth on every new **F** gate.
