# Changelog

All notable changes to the AZL Language project are documented here. AZL is a component-based, event-driven language (see [docs/language/AZL_LANGUAGE_RULES.md](docs/language/AZL_LANGUAGE_RULES.md)).

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

- **Local workspace layout:** `.azl/benchmarks/`, `.azl/state/`, `.azl/bundles/`, `.azl/quarantine/` (see **`docs/LOCAL_WORKSPACE_LAYOUT.md`**); **`scripts/azl_local_layout.sh`** exports `AZL_BENCHMARKS_DIR` / `AZL_STATE_DIR` / …; **`scripts/azl_migrate_local_workspace.sh`** moves flat `benchmark_*`, state JSONL, root **`quarantine/`**; **`tools/azl_native_engine.c`** writes audit/run JSONL under **`AZL_STATE_DIR`** (default `.azl/state`). **`run_enterprise_daemon.sh`** always **`cd`’s** to repo root then sources layout. CI artifact path **`.azl/benchmarks/benchmark_native_api_*.txt`**.
- **LLM GPU alignment:** **`AZL_LLM_GPU_LAYERS`** aliases **`AZL_LLAMA_NGL`** for llama.cpp **`n_gpu_layers`** / **`-ngl`**; embedded model cache invalidates when layer count changes; **`GET /api/llm/capabilities`** adds **`llm_n_gpu_layers`**, **`llm_n_gpu_layers_env_set`**, **`llm_gpu_stack`**; **`exec_bridge`** seeds GPU/LLM env into **`::internal`**; **`neural.llm.native_infer_gpu_hint`** on **`::neural.core`**; **`docs/AZL_GPU_NEURAL_QUANTUM_INVENTORY.md` §2.1** + **`OPERATIONS.md`** explain training vs inference GPU.
- **In-process GGUF (optional):** `tools/azl_gguf_infer_llamacpp.cpp` + CMake project `tools/cmake/azl_llamacpp_engine` + **`scripts/build_azl_native_engine_with_llamacpp.sh`** (`LLAMA_CPP_ROOT`) link **llama.cpp** into **`.azl/bin/azl-native-engine`**. **`POST /api/llm/gguf_infer`** uses embedded inference unless **`AZL_GGUF_USE_CLI=1`**; **`GET /api/llm/capabilities`** reports **`gguf_embedded_llamacpp`** / **`error: null`**. Default **`build_azl_native_engine.sh`** unchanged (gcc-only). **`docs/NATIVE_LLM_INDEPENDENCE_CODE_AUDIT.md`**; **`verify_native_runtime_live.sh`** accepts both capability shapes; **`LLM_INFRASTRUCTURE_AUDIT.md`** / **`model_loader.azl`** updated.
- **Documentation canon:** [docs/AZL_DOCUMENTATION_CANON.md](docs/AZL_DOCUMENTATION_CANON.md) §1.3 — rows for **LLM proof** scripts (minimal + enterprise bundle), **sysproxy `getenv` / `host_getenv`**; §3 — **`AZL_NATIVE_EXEC_CMD`** vs **`run_enterprise_daemon.sh`** / **`start_azl_native_mode.sh`** footgun. [docs/LLM_INFRASTRUCTURE_AUDIT.md](docs/LLM_INFRASTRUCTURE_AUDIT.md) §3 — **sysproxy `getenv`** row.
- **LLM proof (partner-facing):** `scripts/proof_llm_python_vs_azl.py` + `scripts/run_proof_llm_python_vs_azl.sh` — default **1000** timed attempts per path (Python direct Ollama vs AZL native `POST /api/ollama/generate`), same JSON body; Markdown report under `.azl/proof_llm_python_vs_azl_*.md` with **AZL/Python** mean and p95 ratios. Documented in `OPERATIONS.md`.
- **LLM proof (enterprise bundle loaded):** `scripts/build_enterprise_combined.sh` + `scripts/run_proof_llm_enterprise_bundle.sh` — loads the **fat** enterprise `.azl` into the runtime (quantum/LHA3/neural/AZME list aligned with `run_enterprise_daemon.sh`), then runs the same Python vs native-proxy comparison; report **`PROOF_REPORT_DISCLAIMER`** explains C **`/api/ollama/generate`** vs AZL **`/v1/chat`**. Env **`PROOF_REPORT_TITLE`** / **`PROOF_REPORT_DISCLAIMER`** supported in `proof_llm_python_vs_azl.py`.
- **Host env via sysproxy:** `tools/sysproxy.c` **`getenv`** op (JSON `name` → escaped `value`); **`azl/system/azl_system_interface.azl`** **`export fn host_getenv`**, syscall **`proc.getenv`** (capability **`proc.env`**, default-allow policy unchanged); **`azl/host/exec_bridge.azl`** single **`init`**, **`link ::azl.system_interface`**, **`merge_host_env_into_internal`** seeds **`::internal`** from the real process environment; **`azl/core/internal.azl`** drops placeholder empty strings for host keys (defaults only for port/API flags).
- **P0d spine (minimal contract):** Parenthesized **`.toInt()`** in **`tools/azl_interpreter_minimal.c`** and **`tools/azl_semantic_engine/minimal_runtime.py`** (integer parse; empty/invalid → **0**). **`azl/tests/p0_semantic_interpreter_slice.azl`** extended to match **`azl_interpreter.azl`** perf init (**`AZL_PARSE_CACHE`**, **`::perf.stats`**, **`::perf.expr_cache`**). Removed duplicate **`set ::dispatch_stack` / `::recursion_depth`** in **`azl_interpreter.azl`**. [RUNTIME_SPINE_DECISION.md](docs/RUNTIME_SPINE_DECISION.md) **P0d**; gate **F3** still green.
- **Doc dedup:** Merged **`docs/AZL_ENTERPRISE_SETUP.md`** into **[ENTERPRISE_BUILD_SYSTEM.md](docs/ENTERPRISE_BUILD_SYSTEM.md)**; removed **`docs/PRODUCTION_RUN.md`** (stale `cargo` path), **`docs/ADVANCED_OPTIMIZATION_FEATURES.md`** (duplicate/conflicting “implemented optimizations” vs **[advanced_features.md](docs/advanced_features.md)**). [AZL_DOCUMENTATION_CANON.md](docs/AZL_DOCUMENTATION_CANON.md) §2 lists retired paths.
- **Documentation canon:** [docs/AZL_DOCUMENTATION_CANON.md](docs/AZL_DOCUMENTATION_CANON.md) — single source for **shipped & verified** work, strength-bar pillars, completed work-queue rows, full documentation map, and pointers to open milestones. Retired **`docs/WORK_QUEUE.md`**, **`docs/STATUS.md`**, **`docs/AZL_STRENGTH_BAR.md`** (content merged). [docs/README.md](docs/README.md) lists it first; cross-links updated in [README.md](README.md), [CONTRIBUTING.md](docs/CONTRIBUTING.md), [RELEASE_READY.md](RELEASE_READY.md), [PROJECT_COMPLETION_ROADMAP.md](docs/PROJECT_COMPLETION_ROADMAP.md), [RUNTIME_SPINE_DECISION.md](docs/RUNTIME_SPINE_DECISION.md), `scripts/verify_azl_strength_bar.sh`.
- **Strength bar (script):** **`scripts/verify_azl_strength_bar.sh`** runs native gates + **`verify_native_runtime_live.sh`** with **`ERROR[AZL_STRENGTH_BAR]:`** on failure. Human-readable pillars: **AZL_DOCUMENTATION_CANON.md §1.7**. **AUDIT_STRENGTH_ITEMS.md:** LSP row **HAVE** (matches shipped `azl_lsp.py`).
- **Port picker:** **`scripts/benchmark_native_api.sh`** (and verify’s port probe) treat **`http_code` 000 / empty** as a free port; any other code means the port is taken.
- **verify_native_runtime_live.sh:** runs **`azl-native-engine`** + **minimal** bootstrap bundle (`c_minimal_link_ping`), waits for **`/healthz`** + **`/readyz`** HTTP **200**, then checks capabilities and native-only `scripts/azl` block; **trap** stops the engine on exit. (Replaces full **`start_azl_native_mode`** here so the live probe matches gate F / LLM bench and is not blocked by unrelated enterprise interpreter failures.)
- **llama-server proxy (model loaded once):** `POST /api/llm/llama_server/completion` on **`azl-native-engine`** — when **`AZL_LLAMA_SERVER_URL`** is set (e.g. `http://127.0.0.1:PORT`), forwards the JSON body to **`$AZL_LLAMA_SERVER_URL/completion`** via **`curl`**; **`GET /api/llm/capabilities`** adds **`llama_server_http_proxy`**, **`llama_server_completion_path`**, **`llama_server_upstream_configured`**. Scripts: **`scripts/run_benchmark_llama_server.sh`**, **`scripts/benchmark_llm_llama_server.py`** (direct **`llama-server`** vs AZL proxy). **`tools/azl_native_engine.c`:** reads full HTTP POST bodies per **`Content-Length`** (fixes split-packet truncation that could skew proxy latency).
- **Direct GGUF (no Ollama):** `POST /api/llm/gguf_infer` on **`azl-native-engine`** — **`AZL_GGUF_PATH`** + **`llama-cli`** (llama.cpp) fork/exec per request; `GET /api/llm/capabilities` gains **`gguf_cli_infer`** / **`gguf_model_configured`** and **`ERR_NATIVE_GGUF_NOT_IN_PROCESS`**. Scripts: **`scripts/run_benchmark_gguf_direct.sh`**, **`scripts/benchmark_llm_gguf_direct.py`** (Python `llama-cli` vs AZL HTTP). **`verify_native_runtime_live.sh`** updated for the new error code.
- **GGUF infer polish:** stderr → `/dev/null` in child, drop **`--log-disable`** (it hid stdout on `llama-completion`); optional **`AZL_LLAMA_SIMPLE_IO=1`**; **`benchmark_llm_gguf_direct.py`** stderr handling fix.
- **LLM bench:** `LLM_BENCH_WARMUP` + `LLM_BENCH_NUM_PREDICT` for `benchmark_llm_ollama.sh` / `benchmark_llm_python_client.py` (fairer Python vs curl vs AZL); Python p50/p95 index aligned with shell percentiles; run log default **`.azl/benchmarks/benchmark_llm_python_run.log`** (via `AZL_BENCHMARKS_DIR`).
- **P0c interpreter slice:** Widened `azl/tests/p0_semantic_interpreter_slice.azl` toward `::azl.interpreter` init: `::internal.env("…")` (real env), **`or` / `==` / `!=` / `(` `)` / `null`**, **`if EXPR { … }`**, keyed **`{ … }`** aggregate for `::perf` (still stored as minimal `"{}"`), plus prior `set`/`link`/`say`/`P0_SEMANTIC_INTERPRETER_SLICE_OK`. Tokenizer **`==`** / **`!=`**. **Minimal `exec_set`:** non-aggregate RHS uses the same expression evaluator as **`if`**; parse errors exit **5** (C) / **`SemanticEngineError(5)`** (Python, surfaced by `run_file`). Native gate **F3**; `RUNTIME_SPINE_DECISION.md` / `WORK_QUEUE.md` updated.
- **Full verification:** `scripts/run_full_repo_verification.sh` — `RELEASE_READY` sequence + `run_all_tests.sh` + optional Ollama + enterprise `/v1/chat` benches (`RUN_OPTIONAL_BENCHES=0` to skip). `RELEASE_READY.md`, `README.md`, `OPERATIONS.md`, `WORK_QUEUE.md` updated.
- **Work queue + HTTP profile:** `docs/WORK_QUEUE.md` (ordered checklist); `docs/CANONICAL_HTTP_PROFILE.md` (C engine vs enterprise `http_server`). Roadmap “next actions” points at WORK_QUEUE; docs index updated.
- **Gate H extended:** `verify_p0_interpreter_tokenizer_boundary.sh` — full-file **`{`/`}` token balance** + `component ::azl.interpreter` anchor on `azl_interpreter.azl`.
- **Product suite step 1:** optional **`.azl/local_api_token`** (first line) for `AZL_API_TOKEN`; **`benchmark_enterprise_v1_chat.sh`** probes **`POST /v1/chat`** and exits **95** on **404** (wrong server on port). **Next actions (ordered)** in `docs/PROJECT_COMPLETION_ROADMAP.md`.
- **P0 gate H:** `scripts/verify_p0_interpreter_tokenizer_boundary.sh` — semantic minimal tokenizer ingests `azl/runtime/interpreter/azl_interpreter.azl` (full file today; lex milestone toward executing it). Wired into `scripts/check_azl_native_gates.sh`.
- **Product benchmark suite:** `scripts/run_product_benchmark_suite.sh` — runs native LLM bench + optional enterprise `/v1/chat` when `AZL_API_TOKEN` is set; documented in `RELEASE_READY.md`, `README.md`, `OPERATIONS.md`, `CONTRIBUTING.md`.
- **Enterprise chat benchmark:** `scripts/benchmark_enterprise_v1_chat.sh` — `POST /v1/chat` latency against the enterprise HTTP stack (requires `AZL_API_TOKEN`; rejects C-native ports). Documented in `LLM_INFRASTRUCTURE_AUDIT.md`, `CONTRIBUTING.md`, `README.md`, `OPERATIONS.md`, `docs/README.md`; `PROJECT_COMPLETION_ROADMAP.md` Layer 2 updated with shipped instrumentation vs open “canonical profile” choice.
- **Advanced topology handshake:** `on initialize_advanced_topology` now validates payload strictly (`advanced_topology` must be boolean `true`, `consciousness_integration` must be boolean); on failure emits `quantum.mathematics.advanced_topology.error` plus `log_error` (no silent defaults).
- **LLM bench automation:** `scripts/run_native_engine_llm_bench.sh` — build/start C `azl-native-engine` with minimal bootstrap, then run `benchmark_llm_ollama.sh`; documented in `docs/LLM_INFRASTRUCTURE_AUDIT.md` §5.
- **Rename:** `azl/quantum/mathematics/quantum_math.azl` → `advanced_topology.azl` (matches `::quantum.mathematics.advanced_topology`). `train_real_models.azl`, `azl.build.json`, reorganizer plan, architecture docs, and inventory/audit markdown updated; `on initialize_advanced_topology` added for orchestration handshake.
- **Quantum mathematics audit:** `docs/AZL_GPU_NEURAL_QUANTUM_INVENTORY.md` §8 — per-file verdicts for `azl/quantum/mathematics/*.azl` (scaffold vs partial real stepping in chaos module); `DEEP_AUDIT_QUANTUM_MEMORY_PHYSICS.md` gaps list updated. Audit script lists `quantum/mathematics` line counts.
- **GitHub-facing docs:** `README.md` rewritten for accurate native spine, LLM proxy honesty, library-vs-default-runtime scope; removed marketing tagline; `docs/CONTRIBUTING.md` links research-library policy.
- **Capability inventory:** `docs/AZL_GPU_NEURAL_QUANTUM_INVENTORY.md` maps GPU/device/neural/LHA3/quantum `.azl` surfaces vs default native runtime; `scripts/audit_gpu_neural_quantum_surfaces.sh` prints counts and GPU-pattern file hits. Clarified headers: `azl/quantum/real_quantum_processor.azl` (CPU state-vector sim), `azl/core/types/tensor.azl` (no GPU binding).
- **Runtime spine selector:** `AZL_RUNTIME_SPINE` (`c_minimal` default, or `azl_interpreter` / `semantic`) chooses default `AZL_NATIVE_RUNTIME_CMD` via `scripts/azl_resolve_native_runtime_cmd.sh`; semantic path uses `scripts/azl_azl_interpreter_runtime.sh` → `tools/azl_runtime_spine_host.py` → **`tools/azl_semantic_engine/minimal_runtime.py`** (Python, parity with C minimal on the supported subset). Gate **F2** (stdout byte-match vs C on `c_minimal_link_ping.azl`), gate **G** + `scripts/verify_runtime_spine_contract.sh`. Full `azl_interpreter.azl` self-host still roadmap — `docs/PROJECT_COMPLETION_ROADMAP.md`.
- **Repository hygiene:** `.gitignore` now ignores the entire `.azl/` directory; all previously tracked paths under `.azl/` were removed from the index (`git rm --cached`). Local builds and daemons keep using `.azl/` on disk; it is no longer part of the Git tree. Use `git add -f .azl/...` only for deliberate exceptions.
- **Runtime spine (source of truth):** [docs/RUNTIME_SPINE_DECISION.md](docs/RUNTIME_SPINE_DECISION.md) — C engine orchestrates; AZL interpreter is the decided semantic core; documents **current vs target**, **P0 not done** on default `start_azl_native_mode.sh` path, ordered **P0–P5** obligations with file pointers; quantum core-language one-liner; GGUF deferred unless product requires it.
- Documentation and GitHub readiness: LICENSE, CONTRIBUTING (Python + AZL), AZL_LANGUAGE_RULES, GRAMMAR reference, updated README and project structure, clarified advanced_features as future-only.
- Documentation cleanup: removed obsolete root/`reports`/`azl/docs` status and “supervisor” markdown that contradicted the native AZL stack (stale Rust-runtime claims, duplicate completion reports). Canonical index: `docs/README.md`; CI truth: `docs/CI_CD_PIPELINE.md`; short status: `docs/STATUS.md`.
- **`AZL_USE_VM=1`**: `::azl.interpreter` `execute` path can compile a restricted AST (say/emit + init-only components) to linear ops and run `vm_run_bytecode_program` with shared `emit_event_resolved` dispatch; documented in `docs/AZL_NATIVE_RUNTIME_CONTRACT.md`; verified by `scripts/test_azl_use_vm_path.sh` (includes `verify_azl_use_vm_path.sh`, `check_azl_vm_tree_parity.py`, fixture `azl/tests/fixtures/vm_parity_minimal.azl`). Contract clarifies default enterprise **C minimal** runtime does not execute the AZL interpreter.
- **Native LLM capabilities:** `GET /api/llm/capabilities` on `azl-native-engine` reports Ollama proxy vs GGUF status (error code **`ERR_NATIVE_GGUF_NOT_IN_PROCESS`** for “not in-process”); `verify_native_runtime_live.sh` + `check_azl_native_gates.sh` enforce it; `docs/LLM_INFRASTRUCTURE_AUDIT.md` synced to sysproxy/C engine HTTP reality.
- **`.azlpack` dogfood:** `packages/src/azl-hello/` first-party pack, `scripts/build_azlpack.sh`, local install via `AZL_REGISTRY_DIR` in `scripts/azl_install.sh`, `scripts/verify_azlpack_local.sh` in full test run. **LSP:** `tools/azl_lsp.py` — `textDocument/definition` (`::` components, `emit`↔`listen`, `fn`/`function`); `scripts/verify_lsp_smoke.sh` + `scripts/test_lsp_jump_to_def.sh`; fixture `azl/tests/lsp_definition_resolution.azl`.
- **Integrations clarity:** `docs/INTEGRATIONS_HOST_VS_NATIVE.md`, `azl/integrations/anythingllm/README.md`, pure-AZL `azme_ollama_native.azl` (Ollama via `syscall` `http`), `scripts/verify_native_bundle_excludes_host_integrations.sh` in full test run.
