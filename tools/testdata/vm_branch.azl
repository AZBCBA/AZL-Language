# Native compiler: if/else with variable-backed emit payload on success branch.
set status = "ok"
if ( status == "ok" ) {
  emit "success" { "result": status }
} else {
  emit "failure" { "result": "no" }
}
